/**
 * Exporte les constantes globales
 * Permet les imports simplifiés: import { colors, config } from '../constants'
 */

export { colors, cssVariables } from './colors';
export { messages } from './messages';
export { config } from './config';
